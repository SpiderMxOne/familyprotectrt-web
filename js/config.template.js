/**
 * Plantilla de Configuración de FamilyProtect
 *
 * INSTRUCCIONES:
 * 1. Copia este archivo y renombralo a 'config.js' en la misma carpeta.
 * 2. Reemplaza los valores de 'PLACEHOLDER' con tus credenciales reales de Firebase y Google Maps.
 * 3. IMPORTANTE: Si usas Git, añade 'config.js' a tu archivo .gitignore para no subir tus llaves al repositorio.
 */

window.FP_CONFIG = {
    firebase: {
        apiKey: "TU_API_KEY",
        authDomain: "TU_PROYECTO.firebaseapp.com",
        projectId: "TU_PROYECTO",
        storageBucket: "TU_PROYECTO.appspot.com",
        messagingSenderId: "TU_SENDER_ID",
        appId: "TU_APP_ID",
        measurementId: "TU_MEASUREMENT_ID"
    },
    googleMaps: {
        apiKey: "TU_GOOGLE_MAPS_API_KEY"
    }
};
